TovarBot - Telegram магазин цифровых товаров
Описание проекта
Современный Telegram-бот на aiogram 3.x для продажи цифровых товаров с полным функционалом интернет-магазина. Проект имеет чистую архитектуру с разделением на слои и полную реализацию коммерческого функционала.
Текущее состояние проекта
Статус: Активная разработка (обновлен 17 часов назад)
Готово: Основной функционал бота, админ-панель, реферальная система
В работе: Автоматическая выдача товаров, платежные системы
Техническая архитектура
Стек технологий

Язык: Python 3.9+
Фреймворк бота: aiogram 3.x (современная асинхронная версия)
База данных: SQLite (по умолчанию) / PostgreSQL (опционально)
ORM: SQLAlchemy (асинхронная)
Архитектура: Clean Architecture с разделением на слои

Структура проекта (слоистая архитектура)
tovarbot/
├── main.py                 # Точка входа и инициализация бота
├── config.py              # Конфигурация и переменные окружения
├── requirements.txt       # Python зависимости
├── .env.example          # Шаблон переменных окружения
│
├── database/             # Слой данных (Data Layer)
│   ├── models.py         # SQLAlchemy модели
│   └── database.py       # Подключение к БД и сессии
│
├── repositories/         # Слой доступа к данным (Repository Pattern)
│   ├── user_repository.py
│   ├── product_repository.py
│   ├── order_repository.py
│   └── category_repository.py
│
├── services/            # Бизнес-логика (Business Logic Layer)
│   ├── user_service.py
│   ├── product_service.py
│   ├── order_service.py
│   └── referral_service.py
│
├── handlers/            # Обработчики Telegram (Presentation Layer)
│   ├── user_handlers.py
│   ├── admin_handlers.py
│   └── callback_handlers.py
│
├── keyboards/           # UI компоненты
│   └── inline_keyboards.py
│
└── utils/              # Вспомогательные модули
    ├── states.py       # FSM состояния для диалогов
    └── formatters.py   # Форматирование сообщений
Ключевые модели данных

User: пользователи с балансом, реферальной системой и промокодами
Category: категории товаров для организации каталога
Product: товары с остатками и цифровым контентом
Order: заказы со статусами и автоматической выдачей
Referral: реферальная система с вознаграждениями

Основной функционал
Пользовательские возможности

🛍 Каталог товаров с категориями и поиском
💰 Система внутреннего баланса
🛒 Корзина заказов с детальной информацией
👥 Реферальная программа с автоматическими выплатами
🎟 Персональные промокоды для скидок
🆘 Встроенная поддержка через Telegram

Административные функции

⚙️ Полная админ-панель для управления
📊 Детальная статистика продаж и пользователей
🎯 Ручная выдача цифровых товаров
📋 Управление заказами (подтверждение/отмена)
👥 Просмотр активности всех пользователей

Особенности архитектуры
Паттерны проектирования

Repository Pattern: изоляция логики доступа к данным
Service Layer: бизнес-логика вынесена в отдельный слой
Dependency Injection: слабая связанность между компонентами
FSM (Finite State Machine): управление состояниями диалогов

Принципы разработки

Clean Architecture: четкое разделение ответственности
SOLID принципы: особенно Single Responsibility и Dependency Inversion
Async/Await: полная асинхронность для высокой производительности
Type Hints: строгая типизация для лучшей читаемости

Переменные окружения
envBOT_TOKEN=your_bot_token_here           # Токен Telegram бота
ADMIN_IDS=123456789,987654321           # ID админов через запятую
DATABASE_URL=sqlite+aiosqlite:///./bot.db  # URL базы данных
DEBUG=True                              # Режим отладки
REFERRAL_REWARD_PERCENT=10.0           # Процент реферальной награды
SUPPORT_USERNAME=your_support_username  # Username поддержки
EARNING_CHANNEL=https://t.me/channel    # Канал для заработка
Бизнес-процессы
Процесс покупки

Пользователь выбирает категорию из каталога
Выбирает конкретный товар из списка
Подтверждает заказ в корзине
Средства автоматически списываются с баланса
Админ получает уведомление о новом заказе
Админ вручную выдает цифровой товар (пока что)
Пользователь получает контент в личные сообщения

Реферальная система

Автоматическая генерация уникальных реферальных кодов
Начисление процента с каждой покупки рефералов
Детальная статистика по привлеченным пользователям
Возможность вывода реферальных средств

Команды бота

/start - Регистрация и приветствие
/menu - Главное меню с каталогом
/profile - Личный профиль и статистика
/help - Справочная информация
/admin - Админ-панель (только для админов)

Безопасность и валидация

Проверка прав доступа для всех админ-функций
Валидация пользовательского ввода на всех этапах
Безопасное хранение токенов в переменных окружения
Полная обработка ошибок и исключений
Детальное логирование всех операций

Планы развития (roadmap)

Высокий приоритет: Автоматическая выдача цифровых товаров
Высокий приоритет: Интеграция с платежными системами (ЮKassa, Stripe)
Средний приоритет: Веб-панель администратора
Средний приоритет: Расширенная система скидок и промокодов
Низкий приоритет: REST API для внешних интеграций

Контекст для AI-ассистента
Архитектурные предпочтения

Строго следую принципам Clean Architecture
Предпочитаю явное разделение слоев ответственности
Важна читаемость и поддерживаемость кода
Использую type hints везде где возможно

Что делать при разработке

Следовать существующей архитектуре слоев
Добавлять новую логику в соответствующие services
Использовать Repository pattern для работы с данными
Обрабатывать все возможные исключения
Добавлять подробные комментарии к сложной логике

Что НЕ делать

Не мешать слои архитектуры (handlers не должны работать с БД напрямую)
Не добавлять бизнес-логику в handlers или repositories
Не использовать синхронные операции с БД
Не игнорировать обработку ошибок
Не забывать про type hints и docstrings

Стиль кодирования

Использую async/await везде где возможно
Предпочитаю descriptive имена переменных и функций
Следую PEP 8 для форматирования
Добавляю docstrings для всех публичных методов

Особенности проекта

Это коммерческий проект с реальными деньгами - критична надежность
Планируется масштабирование - код должен быть расширяемым
Важна производительность - используем асинхронность максимально
Безопасность критична - все входные данные валидируются
